{
  "type": "object",
  "properties": {
    "enabled": {
      "type": "boolean",
      "title": "Enable content pack",
      "description": "When enabled, this plugin registers its static content.",
      "default": false,
      "ui": {"control": "switch", "order": 10}
    }
  }
}
